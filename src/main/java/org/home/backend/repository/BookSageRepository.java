package org.home.backend.repository;

import org.home.backend.model.BookSage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BookSageRepository extends JpaRepository<BookSage, Long> {
    //Nothing
}
